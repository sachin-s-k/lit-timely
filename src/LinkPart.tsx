const LinkPart = () => {
  // const userData = useSelector((state: any) => state.registration.userData);
  return (
    <>
      <div className="  mt-5">
        {/* Avatar and Name */}
        <div className="flex justify-between"></div>

        {/* Divider */}
        <div className="w-full shadow-lg mt-2 b"></div>
      </div>
    </>
  );
};

export default LinkPart;
